#!/bin/bash

ORIGDIR="$PWD"

pola-nice() {
    POLA_OPTS=""
    if [ -e /lib64 ]
    then
        POLA_OPTS="$POLA_OPTS -f=/lib64"
    fi
    if [ -e /etc/alternatives ]
    then
        POLA_OPTS="$POLA_OPTS -f=/etc/alternatives"
    fi

    pola-run -B $POLA_OPTS -f=/proc -fw=. \
        --prog=nice -a=-n10 \
        -fa="$ORIGDIR"/slox/slox -a=25 \
        -e "$@"
}

#pola-nice() {
#    nice -n10 "$ORIGDIR"/slox/slox 25 "$@"
#}
